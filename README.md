# ai_kampo_app

A new Flutter project.

## Build essentials

This project uses json_serializable for json encode/decode, please make sure all .g.dart files is generated before deployment to git.<br>
<br>
command to generate generated json functions:
```
dart run build_runner build --delete-conflicting-outputs
```
